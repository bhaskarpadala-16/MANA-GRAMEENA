# Product Requirements Document (PRD)

<!--
Ralph Loop Task List:
Write your task list below, one task per line.
Ralph Loop works best when each PRD task has a clear success condition.
Example:
- Set up database schema with users and posts tables
- Implement POST /api/auth/register endpoint returning JWT token on success and 400 on duplicate user
- Implement POST /api/auth/login endpoint returning JWT token on success and 401 on failure
-->

